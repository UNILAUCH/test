# Getting started
1. Install Python 3.10 (version is important)
2. Install Jupyter notebook
3. Install requirements: `pip install -r requirements.txt`
4. Open network_interface in Jupyter Notebook (use `jupyter notebook --notebok-dir=<path>` to set the home directory to this folder)
5. Run the notebook (caffe should be automatically found in the folder)